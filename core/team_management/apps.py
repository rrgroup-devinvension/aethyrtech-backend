from django.apps import AppConfig


class TeamManagementConfig(AppConfig):
    """App config for team management."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'core.team_management'
