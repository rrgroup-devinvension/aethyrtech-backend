from django.apps import AppConfig


class IdentityCloudConfig(AppConfig):
    """Configuration for the identity_cloud module."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'identity_cloud'
