from django.apps import AppConfig


class MediaCloudConfig(AppConfig):
    """Configuration for the media_cloud module."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'media_cloud'
